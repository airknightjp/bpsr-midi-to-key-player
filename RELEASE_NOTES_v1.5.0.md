## 概要

v1.5.0では、MIDIフォルダ管理、演奏表示、再生処理の効率と安定性を改善しました。

## 主な変更

- 選択フォルダ配下のサブフォルダを再帰的に読み込み、MIDI一覧へフォルダ階層を表示します。
- MIDI一覧の列幅変更と保存に対応し、リロード時は追加、削除、更新されたファイルだけを処理します。
- 選択中MIDIの最終出力音域を鍵盤と音ゲー欄へ表示します。
- MIDI入力変換とリアルタイム入力変換で、キー入力と同時に変換音を再生できるようにしました。
- 本アプリまたは子ウィンドウがフォーカス中は、本アプリからのキー入力送信を抑止します。
- ログタブ、スコア、コンボ表示を削除し、タイミング判定と演出だけを残しました。
- 再生計画、MIDI一覧、演奏表示、音声処理の不要な更新を削減しました。
- カウントダウンの初期値を0秒へ変更しました。

## 配布ファイル

`BPSR_MIDI_to_KEY_Player_v1.5.0.zip`

ZIP SHA-256:
`ZIP_SHA256`

EXE SHA-256:
`EXE_SHA256`

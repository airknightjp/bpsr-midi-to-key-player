## 概要

v1.4.0では、GitHub Releasesを利用した自動更新機能を追加し、更新確認からダウンロード、検証、適用、再起動までをアプリ内から行えるようにしました。

## 主な変更

- 起動時にGitHub Releases APIで最新バージョンを確認し、更新がある場合だけ通知します。
- `その他 > 更新を確認`から、いつでも手動で更新を確認できます。
- 自動更新確認は前回の確認から1時間に1回までに制限し、最終確認時刻を`settings.json`へ保存します。手動確認は時間制限に関係なく実行できます。
- 更新前に確認ダイアログを表示し、ダウンロード、検証、適用、再起動の進捗を1つのウィンドウで表示します。
- 配布ZIPのファイルサイズ、SHA-256、構成、パスの安全性を検証してからアプリのファイルを置き換えます。
- 展開先の`settings.json`を保持したまま更新し、完了後はアプリを自動再起動して前面へ表示します。
- 更新に失敗した場合は以前のアプリファイルへ復旧し、次回起動時にエラー内容を通知します。
- 更新結果の表示を、各テーマに対応した専用ダイアログへ変更しました。

## 配布ファイル

`BPSR_MIDI_to_KEY_Player_v1.4.0.zip`

ZIP SHA-256:
69B88120674D6BED14575A29DCBDD081A22A12CADC4246868290C04BA009C34D

EXE SHA-256:
20034161BD158A1D11A9CF1F2414FC16731DD6D4906D09B29F20D5388E32A24A
